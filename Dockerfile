FROM node:18-alpine

WORKDIR /app

COPY . .
RUN npm ci

ENV MONGODB_URL=
ENV SMART_ENERGY_EPEX_SPOT_AT_ENDPOINT_URL=
ENV KV_REST_API_URL=
ENV KV_REST_API_TOKEN=

RUN npm run build

CMD ["npm", "run", "start"]
